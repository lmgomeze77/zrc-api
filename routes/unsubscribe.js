const express=require("express");const router=express.Router();const SUPABASE_URL=process.env.SUPABASE_URL,SUPABASE_KEY=process.env.SUPABASE_ANON_KEY;router.get("/",async(req,res)=>{const{token}=req.query;if(!token)return res.status(400).send("<h1>Invalid link</h1>");try{const response=await fetch(`${SUPABASE_URL}/rest/v1/subscribers?unsubscribe_token=eq.${token}`,{method:"PATCH",headers:{"Content-Type":"application/json",apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,Prefer:"return=minimal"},body:JSON.stringify({active:false})});if(!response.ok)throw new Error("Supabase update failed");return res.send("<html><body style=\"background:#0a1628;color:#fff;font-family:sans-serif;display:flex;align-items:center;justify-content:center;height:100vh;margin:0\"><div style=\"text-align:center\"><h1 style=\"color:#c9a84c\">Unsubscribed</h1><p>You have been removed from ZRC Morning Intelligence.</p></div></body></html>");}catch(err){console.error(err);return res.status(500).send("<h1>Error</h1>");}});module.exports=router;